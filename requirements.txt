# Core Dagster dependencies
dagster>=1.12.0
dagster-webserver>=1.12.0
dagster-cloud>=1.12.0

# ELT tools
dlt[duckdb,snowflake,bigquery,redshift,postgres,motherduck,databricks]>=0.4.0
github3.py>=4.0.1  # Required for GitHub source
requests>=2.28.0  # Required for various sources
sling>=1.2.0

# Utilities
gitpython>=3.1.0
pyyaml>=6.0
pydantic>=2.0.0
python-dotenv>=1.0.0

# Development tools (optional)
black>=23.0.0
ruff>=0.1.0
pytest>=7.0.0
pytest-cov>=4.0.0

# Install the package itself
-e .
